import React from 'react';
import {style} from './index.scss';

const Header = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Header.propTypes = {

};

export default Header;
